# Use the MongoDB store
org.quartz.jobStore.class=xyz.vopen.mixmicro.components.enhance.schedule.quartz.MongoDBJobStore
# MongoDB URI (optional if 'org.quartz.jobStore.addresses' is set)
org.quartz.jobStore.mongoUri=mongodb://pipeline:pipeline#08039863@dev-middle.hgj.net:27017/pipeline
# comma separated list of mongodb hosts/replica set seeds (optional if 'org.quartz.jobStore.mongoUri' is set)
# org.quartz.jobStore.addresses=host1,host2
# database name
org.quartz.jobStore.dbName=pipeline
# Will be used to create collections like mycol_jobs, mycol_triggers, mycol_calendars, mycol_locks
org.quartz.jobStore.collectionPrefix=scheduler
# thread count setting is ignored by the MongoDB store but Quartz requries it
org.quartz.threadPool.threadCount=1

# turn clustering on:
# org.quartz.jobStore.isClustered=true

# Must be unique for each node or AUTO to use autogenerated:
# org.quartz.scheduler.instanceId=AUTO
# org.quartz.scheduler.instanceId=node1

# The same cluster name on each node:
# org.quartz.scheduler.instanceName=clusterName